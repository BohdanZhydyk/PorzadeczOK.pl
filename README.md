# PorzadeczOK.pl
PorządeczOK – cleaning company landing page (React + SCSS) with SEO and responsiveness.

## Scripts

- `npm start` runs the dev server at `http://localhost:3000`
- `npm run build` builds the production bundle
